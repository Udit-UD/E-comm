import { Box } from "@mui/material";
import {styled} from "@mui/system";

const Flex = styled(Box)({
    display: "flex",
    justifyContent: "center",
    alignItems: "center"
})

export default Flex;